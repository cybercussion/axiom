{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "wordpuzzle",
  "allOf": [{ "$ref": "page-base" }],
  "type": "object",
  "description": "Fill-in-the-blanks. Rendered by src/features/templates/template-wordpuzzle.js. text contains {{blankId}} tokens matching blanks[].id.",
  "properties": {
    "type": { "const": "wordpuzzle" },
    "question": {
      "type": "string",
      "minLength": 1,
      "description": "Question prompt — rendered by src/features/templates/template-wordpuzzle.js."
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "description": "Puzzle text containing {{blankId}} tokens — rendered by src/features/templates/template-wordpuzzle.js."
    },
    "blanks": {
      "type": "array",
      "minItems": 1,
      "description": "Blank definitions matching {{blankId}} tokens in text — read by src/features/templates/template-wordpuzzle.js.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "description": "Blank id matching a {{blankId}} token in text — src/features/templates/template-wordpuzzle.js."
          },
          "answers": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "Accepted answers (ANSWER-KEY FIELD)."
          }
        },
        "required": ["id", "answers"],
        "additionalProperties": false
      }
    }
  },
  "required": ["question", "text", "blanks"]
}
